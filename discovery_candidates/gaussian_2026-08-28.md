# Discovered sources for gaussian -- 2026-08-28

## GOOD (3, 2 confirmed by direct review, 1 flagged as a stub despite passing)

### William and Mary - Gaussian
- URL: https://www.wm.edu/offices/it/services/researchcomputing/using/software/gaussian/
- Keyword hits: 27
- Verified: genuine and substantial -- four execution modes (serial/shared/distributed/hybrid)
  each with real SLURM examples, GaussView X11 setup, licensing notes.

```toml
[[html_sources]]
label = "William and Mary - Gaussian"
url = "https://www.wm.edu/offices/it/services/researchcomputing/using/software/gaussian/"
```

### Delaware IT-RCI - Gaussian on Darwin
- URL: https://docs.hpc.udel.edu/software/gaussian/darwin
- Keyword hits: 24
- Verified: genuine and substantial -- GPU offload details (GAUSS_CDEF/GAUSS_GDEF env vars),
  Slurm job template path, input-file handling. Note: the earlier gaussian discovery run
  (2026-08-27) rejected Delaware's hub page (docs.hpc.udel.edu/software/gaussian/gaussian) as a
  thin landing page pointing to 3 cluster-specific sub-pages -- this is one of those sub-pages,
  and it holds up well. The other two (Caviness, Farber) haven't been checked yet.

```toml
[[html_sources]]
label = "Delaware IT-RCI - Gaussian on Darwin"
url = "https://docs.hpc.udel.edu/software/gaussian/darwin"
```

### Lafayette College - HPC Software -- NOT RECOMMENDED despite GOOD verdict (highest hit count of the three)
- URL: https://help.lafayette.edu/hpc-software/
- Keyword hits: 37
- Verified by direct fetch: despite the highest keyword count in this batch, this is a stub --
  the entire Gaussian section is 6 lines (module load + access request), no input file guidance,
  no memory/CPU recommendations, no examples. Listed for completeness rather than omitted;
  recommend skipping.

## Not usable

1 candidate came back EMPTY: Minnesota Supercomputing Institute (www-archive.msi.umn.edu,
no keyword matches -- likely a stale archived page or JS-rendered). 2 came back FAIL: Purdue
RCAC Gilbreth (HTTP 404), UVA Research Computing (HTTP 403).

## Nothing was added automatically

This file is a proposal only. `configs/gaussian.toml` and `gaussian_scraper/presets.py` were
not touched. To accept a candidate, copy its TOML block above into `configs/gaussian.toml`.
Note that `presets.py` isn't kept in sync with `configs/*.toml` automatically -- add accepted
sources there too for consistency with future wizard runs.
